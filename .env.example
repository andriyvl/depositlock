# DepositLock (Next.js app + Hardhat) – Environment Variables Example
# Copy this file to:
# - .env.local (for Next.js runtime; public values only, prefixed with NEXT_PUBLIC_)
# - .env       (for Hardhat deploy/tests; private values like PRIVATE_KEY)
# After changes, restart `pnpm dev` for Next.js.

############################################
# Next.js (client) – PUBLIC variables (env.local)
############################################
# 1) Web3Modal / WalletConnect Cloud Project ID (public)
#    Used to initialize Web3Modal (WalletConnect) to enable wallet connections.
#    Create a project at: https://cloud.walletconnect.com
#    Project → Settings → “Project ID”
#### DEPRECATED
NEXT_PUBLIC_WC_PROJECT_ID=


# Get On dashboard.reown.com
NEXT_PUBLIC_REOWN_PROJECT_ID=

# 2) Polygon Amoy RPC URL (public)
#    HTTPS RPC endpoint for Polygon Amoy (testnet, chainId 80002).
#    Official: https://rpc-amoy.polygon.technology/
#    Alchemy:  https://polygon-amoy.g.alchemy.com/v2/<YOUR_KEY>
#    Infura:   https://polygon-amoy.infura.io/v3/<YOUR_KEY>
NEXT_PUBLIC_RPC_AMOY=

#(optional for websocket)
NEXT_PUBLIC_RPC_AMOY_WEBSOCKET= 

# 3) Escrow Contract Address (public)
#    The deployed escrow contract address used by the app (0x... on Amoy).
NEXT_PUBLIC_CONTRACT_ADDRESS=

############################################
# Hardhat (server) – PRIVATE variables ( IN .env file)
############################################
# 4) Amoy RPC URL for Hardhat (server-side)
#    Same network as above, but not exposed to the browser.
#    Use the same provider URL as NEXT_PUBLIC_RPC_AMOY or a different one.
AMOY_RPC_URL=

# 5) Private key of the deployer account (server-side, KEEP SECRET)
#    EOA private key that will deploy and sign transactions on Amoy.
#    Format: a 0x-prefixed hex string WITHOUT quotes.
#    Example: 0xabc123...deadbeef
PRIVATE_KEY=

# Note on older names: we standardized to NEXT_PUBLIC_RPC_AMOY (client) and AMOY_RPC_URL (server).
# If you previously used POLYGON_AMOY_RPC_URL, it is no longer referenced in the code.
